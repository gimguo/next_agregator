# Концепт: Catalog Builder (Конструктор каталога для витрины)

## 🎯 Идея

**Catalog Builder** — это инструмент для создания и управления каталогом товаров, который будет экспортирован на витрину (ros-matras.ru). Агрегатор собирает данные от поставщиков, а Catalog Builder позволяет:

1. **Выбрать поставщиков** — какие товары включать в каталог
2. **Выбрать шаблон структуры** — как организовать категории
3. **Предпросмотр** — как каталог будет выглядеть на сайте
4. **Экспорт** — отправить готовый каталог на витрину одной командой

---

## 📊 Архитектура данных

### Таблицы

#### 1. `catalog_templates` — Шаблоны каталогов
```sql
id              SERIAL PRIMARY KEY,
name            VARCHAR(255) NOT NULL,           -- "Базовый", "Оптимизированный"
description     TEXT,                            -- Описание шаблона
structure_json  JSONB NOT NULL,                  -- Структура категорий
merge_rules     JSONB,                           -- Правила объединения категорий
is_default      BOOLEAN DEFAULT false,
is_system       BOOLEAN DEFAULT false,           -- Системный (нельзя удалить)
created_at      TIMESTAMP DEFAULT NOW(),
updated_at      TIMESTAMP DEFAULT NOW()
```

**structure_json** — дерево категорий:
```json
{
  "categories": [
    {
      "id": 1,
      "name": "Матрасы",
      "slug": "matrasy",
      "parent_id": null,
      "sort_order": 1,
      "merge_from": [2, 3],  // Какие категории объединить (для оптимизированного)
      "filters": ["stiffness", "size", "spring_block"]
    },
    {
      "id": 2,
      "name": "Пружинные матрасы",
      "slug": "matrasy-pruzhinnye",
      "parent_id": 1,
      "sort_order": 1
    }
  ]
}
```

#### 2. `catalog_previews` — Сохранённые предпросмотры
```sql
id              SERIAL PRIMARY KEY,
template_id     INTEGER REFERENCES catalog_templates(id),
name            VARCHAR(255),                    -- "Каталог Орматек + Аскона"
supplier_ids    INTEGER[] NOT NULL,              -- Массив ID поставщиков
preview_data    JSONB,                            -- Кэш предпросмотра
product_count   INTEGER DEFAULT 0,               -- Всего товаров
category_count  INTEGER DEFAULT 0,               -- Всего категорий
created_by      INTEGER,                          -- user_id
created_at      TIMESTAMP DEFAULT NOW(),
updated_at      TIMESTAMP DEFAULT NOW()
```

**preview_data** — структура для UI:
```json
{
  "categories": [
    {
      "id": 1,
      "name": "Матрасы",
      "slug": "matrasy",
      "product_count": 245,
      "children": [
        {
          "id": 2,
          "name": "Пружинные",
          "product_count": 120
        }
      ]
    }
  ],
  "products_by_category": {
    "1": [101, 102, 103],  // product_model.id
    "2": [104, 105]
  }
}
```

#### 3. `catalog_exports` — История экспортов
```sql
id              SERIAL PRIMARY KEY,
preview_id      INTEGER REFERENCES catalog_previews(id),
status          VARCHAR(20) DEFAULT 'pending',   -- pending, processing, completed, failed
exported_at     TIMESTAMP,
exported_by     INTEGER,                          -- user_id
stats_json      JSONB,                            -- {products: 1000, categories: 50, variants: 2500}
error_message   TEXT,
created_at      TIMESTAMP DEFAULT NOW()
```

---

## 🎨 UI/UX

### Вкладка "Каталог" в сайдбаре

#### 1. Главная страница `/catalog-builder/index`

**Левая панель:**
- Список сохранённых предпросмотров
- Кнопка "Создать новый каталог"

**Правая панель (если выбран предпросмотр):**
- Название
- Шаблон
- Поставщики
- Статистика (товаров, категорий)
- Кнопки: "Предпросмотр", "Экспортировать", "Удалить"

#### 2. Страница создания `/catalog-builder/create`

**Шаг 1: Выбор поставщиков**
```
┌─────────────────────────────────────┐
│ Поставщики                          │
├─────────────────────────────────────┤
│ ☑ Орматек (245 товаров)            │
│ ☑ Аскона (189 товаров)             │
│ ☐ Другие поставщики                │
│                                     │
│ [Выбрать все] [Сбросить]           │
└─────────────────────────────────────┘

Всего выбрано: 2 поставщика, 434 товара
```

**Шаг 2: Выбор шаблона**
```
┌─────────────────────────────────────┐
│ Шаблон каталога                    │
├─────────────────────────────────────┤
│ ○ Базовый                          │
│   Максимально подробный, все        │
│   категории как у поставщиков       │
│                                     │
│ ● Оптимизированный ⭐               │
│   Схожие группы объединены,          │
│   удобная навигация                 │
│                                     │
│ ○ Пользовательский                 │
│   [Создать новый шаблон]            │
└─────────────────────────────────────┘
```

**Шаг 3: Предпросмотр**
```
┌─────────────────────────────────────┐
│ Предпросмотр каталога               │
├─────────────────────────────────────┤
│ 📁 Матрасы (245)                    │
│   📁 Пружинные (120)                │
│   📁 Беспружинные (125)             │
│ 📁 Кровати (89)                     │
│   📁 Прямые (45)                    │
│   📁 Угловые (44)                   │
│                                     │
│ [Редактировать структуру]           │
│ [Сохранить предпросмотр]            │
│ [Экспортировать на витрину]         │
└─────────────────────────────────────┘
```

#### 3. Редактор структуры `/catalog-builder/edit-structure`

- Drag & Drop для перестановки категорий
- Объединение категорий
- Переименование
- Удаление пустых категорий
- Настройка фильтров для категории

---

## 🔧 Логика работы

### 1. Построение каталога из поставщиков

```php
class CatalogBuilderService
{
    /**
     * Строит структуру каталога на основе шаблона и выбранных поставщиков.
     */
    public function buildCatalog(int $templateId, array $supplierIds): array
    {
        $template = CatalogTemplate::findOne($templateId);
        $structure = $template->structure_json;
        
        // 1. Получить все товары выбранных поставщиков
        $models = ProductModel::find()
            ->innerJoin('supplier_offers so', 'so.model_id = product_models.id')
            ->where(['so.supplier_id' => $supplierIds])
            ->andWhere(['product_models.status' => 'active'])
            ->distinct()
            ->all();
        
        // 2. Применить структуру шаблона
        $categories = $this->applyTemplateStructure($structure, $models);
        
        // 3. Применить правила объединения (для оптимизированного)
        if ($template->merge_rules) {
            $categories = $this->applyMergeRules($categories, $template->merge_rules);
        }
        
        // 4. Распределить товары по категориям
        $productsByCategory = $this->distributeProducts($models, $categories);
        
        return [
            'categories' => $categories,
            'products_by_category' => $productsByCategory,
            'total_products' => count($models),
        ];
    }
}
```

### 2. Экспорт на витрину

```php
class CatalogExportService
{
    /**
     * Экспортирует готовый каталог на витрину.
     */
    public function exportToStorefront(int $previewId): bool
    {
        $preview = CatalogPreview::findOne($previewId);
        $data = $preview->preview_data;
        
        // 1. Создать/обновить категории на витрине
        foreach ($data['categories'] as $catData) {
            $this->syncCategory($catData);
        }
        
        // 2. Отправить товары через существующий Outbox
        foreach ($data['products_by_category'] as $categoryId => $modelIds) {
            foreach ($modelIds as $modelId) {
                // Используем существующий механизм синхронизации
                Yii::$app->get('outbox')->emitContentUpdate($modelId);
            }
        }
        
        // 3. Обновить связи товаров с категориями на витрине
        $this->updateProductCategories($data);
        
        return true;
    }
}
```

---

## 📋 Шаблоны каталогов

### Базовый шаблон
- Все категории из MDM как есть
- Максимальная детализация
- Подходит для полного каталога всех поставщиков

### Оптимизированный шаблон
- Объединяет схожие категории:
  - "Матрасы пружинные" + "Матрасы с независимым блоком" → "Пружинные матрасы"
  - "Кровати прямые" + "Кровати с подъёмным механизмом" → "Прямые кровати"
- Упрощённая навигация
- Подходит для быстрого запуска

### Пользовательский шаблон
- Создаётся менеджером
- Можно редактировать структуру
- Сохраняется в `catalog_templates`

---

## 🚀 Этапы реализации

### Этап 1: Базовая структура
- [ ] Миграции для `catalog_templates`, `catalog_previews`, `catalog_exports`
- [ ] AR-модели
- [ ] Контроллер `CatalogBuilderController`
- [ ] Базовый UI: выбор поставщиков + шаблона

### Этап 2: Предпросмотр
- [ ] `CatalogBuilderService::buildCatalog()`
- [ ] Страница предпросмотра с деревом категорий
- [ ] Статистика по категориям

### Этап 3: Редактирование
- [ ] Drag & Drop для категорий
- [ ] Объединение категорий
- [ ] Сохранение пользовательских шаблонов

### Этап 4: Экспорт
- [ ] `CatalogExportService::exportToStorefront()`
- [ ] Интеграция с существующим Outbox
- [ ] История экспортов

---

## 💡 Преимущества

1. **Гибкость** — можно создать несколько каталогов для разных витрин
2. **Контроль** — видно что именно будет на сайте до экспорта
3. **Быстрый запуск** — оптимизированный шаблон для MVP
4. **Расширяемость** — легко добавить новые шаблоны и правила

---

## 🔄 Интеграция с существующей системой

- Использует существующие `ProductModel`, `Category`, `Supplier`
- Использует `OutboxService` для синхронизации товаров
- Использует `RosMatrasSyndicationService` для построения проекций
- Не нарушает текущий workflow агрегатора
